{
  if (this == o) {
    return true;
  }
  if (!(o instanceof CommandResult)) {
    return false;
  }
  CommandResult that=(CommandResult)o;
  return (mStartTime == that.mStartTime && mExitValue == that.mExitValue && mStdout.equals(that.mStdout) && mStderr.equals(that.mStderr) && mEndTime == that.mEndTime);
}
