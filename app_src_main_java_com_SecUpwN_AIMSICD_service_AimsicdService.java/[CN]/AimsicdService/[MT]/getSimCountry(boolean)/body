{
  if (mSimCountry.isEmpty() || force) {
    try {
      mSimCountry=(tm.getSimCountryIso() != null) ? tm.getSimCountryIso() : "N/A";
    }
 catch (    Exception e) {
      Log.e(TAG,"getSimCountry " + e);
    }
  }
  if (mSimCountry.isEmpty())   mSimCountry="N/A";
  return mSimCountry;
}
