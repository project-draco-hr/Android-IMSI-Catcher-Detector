{
  if (mSimOperator.isEmpty() || force) {
    try {
      mSimOperator=(tm.getSimOperator() != null) ? tm.getSimOperator() : "N/A";
    }
 catch (    Exception e) {
      Log.e(TAG,"getSimOperator " + e);
    }
  }
  if (mSimOperator.isEmpty())   mSimOperator="N/A";
  return mSimOperator;
}
