{
  if (mPhoneNum.isEmpty() || force) {
    try {
      mPhoneNum=tm.getLine1Number();
    }
 catch (    NullPointerException npe) {
    }
  }
  if (mPhoneNum.isEmpty())   mPhoneNum=tm.getSubscriberId();
  return mPhoneNum;
}
