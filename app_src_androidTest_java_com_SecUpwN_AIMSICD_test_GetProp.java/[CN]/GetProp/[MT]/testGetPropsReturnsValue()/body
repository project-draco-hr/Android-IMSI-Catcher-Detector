{
  DebugLogs activity=(DebugLogs)getActivity();
  try {
    Thread.sleep(1000);
  }
 catch (  Exception e) {
  }
  String props=activity.getProp();
  Log.d("getprop",props);
  assertTrue(props.trim().length() > 0);
}
