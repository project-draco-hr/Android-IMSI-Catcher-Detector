{
  try {
    mDb.delete("DetectionStrings","det_str='" + deleteme + "'",null);
    return true;
  }
 catch (  Exception ee) {
    Log.i(TAG,mTAG + ": Deleting detection string failed: " + ee);
  }
  return false;
}
