{
  if (!checkDataBase()) {
    this.getReadableDatabase();
    try {
      copyDataBase();
      Log.i(TAG,mTAG + ": Database created");
      return true;
    }
 catch (    IOException e) {
      throw new Error("Error copying database\n" + e.toString());
    }
  }
  return false;
}
