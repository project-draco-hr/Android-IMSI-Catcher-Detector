{
  Cursor cursor=mDb.rawQuery("SELECT 1 FROM " + CELL_TABLE + " WHERE CellID = "+ cellID,null);
  boolean exists=cursor.getCount() > 0;
  Log.i(TAG,"Cell exists in local DB?: " + exists);
  cursor.close();
  return exists;
}
