{
  String query=String.format("SELECT %s, %s, %s FROM  %s ORDER BY %s DESC",DBTableColumnIds.DBI_MEASURE_BTS_ID,DBTableColumnIds.DBI_MEASURE_RX_SIGNAL,DBTableColumnIds.DBI_MEASURE_TIME,DBTableColumnIds.DBI_MEASURE_TABLE_NAME,DBTableColumnIds.DBI_MEASURE_TIME);
  return mDb.rawQuery(query,null);
}
