{
  if (s != null) {
    if (IsConnectedToCdmaFemto(s)) {
      Helpers.msgShort(uniqueContext,"ALERT! FemtoCell Connection Detected.");
      mFemtoDetected=true;
      setNotification();
    }
 else {
      mFemtoDetected=false;
      setNotification();
    }
  }
}
