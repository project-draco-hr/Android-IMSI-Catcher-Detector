{
  NotificationManager notificationManager=(NotificationManager)uniqueContext.getSystemService(Context.NOTIFICATION_SERVICE);
  if (notificationManager != null) {
    notificationManager.cancel(NOTIFICATION_ID);
  }
}
