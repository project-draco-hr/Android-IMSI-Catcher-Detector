{
  String[] buffer=line.split(" ");
  line=String.valueOf(Calendar.getInstance().get(Calendar.YEAR)) + buffer[0] + buffer[1];
  return line.substring(0,line.length() - 4).replace(":","").replace(".","").replace("-","");
}
